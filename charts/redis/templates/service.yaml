apiVersion: v1
kind: Service
metadata:
  name: redis
  labels: { app: redis, project: roboshop, tier: db }
spec:
  selector: { app: redis, project: roboshop, tier: db }
  ports:
    - { protocol: TCP, port: 6379, targetPort: 6379 }
